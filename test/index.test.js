require('../src/index')

describe('a test', () => {
  it('should work', () => {
    expect(1 + 2).toBe(3)
  })

  it('should work', () => {
    expect(2 + 2).toBe(4)
  })

  it('should work', () => {
    expect(2 + 3).toBe(5)
  })
})
